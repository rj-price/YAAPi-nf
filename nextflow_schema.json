{
    "\$schema": "http://json-schema.org/draft-07/schema",
    "\$id": "https://raw.githubusercontent.com/jnprice/YAAPi-nf/master/nextflow_schema.json",
    "title": "YAAPi-nf pipeline parameters",
    "description": "Yeast Assembly and Annotation Pipeline",
    "type": "object",
    "definitions": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output results.",
            "required": ["input"],
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.csv$",
                    "description": "Path to input samplesheet (CSV: sample,fastq_1,fastq_2)",
                    "help_text": "Path to your input samplesheet CSV file. Headers must be: sample,fastq_1,fastq_2",
                    "fa_icon": "fas fa-file-csv"
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory",
                    "default": "./results",
                    "fa_icon": "fas fa-folder-open"
                }
            }
        },
        "database_options": {
            "title": "Database options",
            "type": "object",
            "description": "Paths to various databases required by the pipeline.",
            "properties": {
                "busco_db": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Path to BUSCO database"
                },
                "kraken2_db": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Path to Kraken2 database"
                },
                "mito_db": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Path to Organelle BLAST database"
                },
                "funannotate_db": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Path to Funannotate database"
                }
            }
        },
        "tool_options": {
            "title": "Tool parameters",
            "type": "object",
            "description": "Tune specific tool settings.",
            "properties": {
                "busco_lineage": {
                    "type": "string",
                    "default": "fungi_odb10",
                    "description": "BUSCO lineage to use"
                },
                "kmer_length": {
                    "type": "integer",
                    "default": 21,
                    "description": "K-mer length for analysis"
                },
                "ploidy": {
                    "type": "integer",
                    "default": 2,
                    "description": "Expected ploidy for GenomeScope2"
                },
                "skip_annotation": {
                    "type": "boolean",
                    "default": false,
                    "description": "Skip Funannotate gene prediction"
                }
            }
        }
    },
    "allOf": [
        { "\$ref": "#/definitions/input_output_options" },
        { "\$ref": "#/definitions/database_options" },
        { "\$ref": "#/definitions/tool_options" }
    ]
}
